# Resend API key — powers the landing-page inquiry form (app/api/inquire).
# Create a key at https://resend.com after verifying the aeterna-ehr.com domain,
# then set this in Vercel → Project → Settings → Environment Variables.
RESEND_API_KEY=
